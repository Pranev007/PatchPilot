# Copy to .env and fill in, or export in your shell.
# Get a key at https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Optional: hard spend cap per repo, in USD. The run aborts if exceeded.
MIGRATOR_MAX_SPEND_USD=1.00
